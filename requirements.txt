# Pinned to the versions actually used on the compute host (munchlab1, conda env "ts",
# Python 3.11.16). Regenerate the exact set with:
#     ssh iguzel@35.9.130.228 '~/miniforge3/envs/ts/bin/python -m pip freeze' > requirements-lock.txt

# Data Generation
betise==0.4.0            # 0.4.0 requires a seasonal base_series for contextual_anomaly

# Time Series Feature Engineering
tsfresh==0.21.2
ripser==0.6.15           # persistent homology (Takens + Vietoris-Rips, H1)
persim==0.3.8            # pulled in by ripser; persistence landscapes

# Machine Learning
scikit-learn==1.9.0
xgboost==3.2.0
catboost==1.2.10         # required to reproduce the reported CatBoost results

# Data Processing
pandas==3.0.5
numpy==2.4.6
pyarrow==25.0.1

# Visualization
matplotlib==3.11.1
seaborn==0.13.2

# Statistics & utilities
scipy==1.17.1
statsmodels==0.15.0
joblib==1.6.0
tqdm==4.70.0
